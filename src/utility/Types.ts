export type ProjectsTypes = {
  id: string;
  img: string;
  name: string;
  description: string;
  createdAt: number;
  time: string;
  client: string;
  repo: string;
  role: string[];
  site: string;
};
export type BlogTypes = {
  title: string;
  cover_img: string;
  description: string;
  slug: string;
  tags: string[];
  created_at: string;
};
